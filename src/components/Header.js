import React from 'react';
import Typography from '@material-ui/core/Typography';

function Header() {
  return (
      <header className="App-header">
        <Typography variant="h4">Rick Ekins</Typography>
      </header>
  );
}

export default Header;
